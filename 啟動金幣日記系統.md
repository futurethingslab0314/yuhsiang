# 🪙 金幣日記獎勵系統 - 快速啟動指南

## 📋 功能概述

這個系統整合了：
- **每日定時引導語**：根據使用者歷史日記內容，由 OpenAI 生成個人化引導語
- **語音日記錄製**：錄音後自動上傳並儲存到 Firebase
- **金幣獎勵系統**：每次記錄獲得 150 枚金幣，視覺化堆疊動畫
- **個人化鼓勵訊息**：金幣堆滿時，OpenAI 根據歷史內容生成專屬鼓勵語

## 🚀 第一次啟動（完整步驟）

### 步驟 1：確認環境變數檔案

確保專案根目錄有 `.env` 檔案，內容至少包含：

```bash
# OpenAI API
OPENAI_API_KEY=sk-...你的金鑰

# Firebase Admin SDK
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
...你的完整私鑰...
-----END PRIVATE KEY-----"

# Firebase Web SDK (選用，如需前端直接存取)
FIREBASE_API_KEY=AIza...
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_STORAGE_BUCKET=your-project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789012
FIREBASE_APP_ID=1:123456789012:web:...
```

### 步驟 2：安裝依賴

```bash
cd ~/yuhsiang
npm install
```

### 步驟 3：啟動本地伺服器

```bash
node server.js
```

看到以下訊息代表成功：
```
✅ 已載入環境變數
🚀 伺服器已啟動！
📍 網址: http://localhost:3000/diary-reward.html
📡 API: http://localhost:3000/api/...
```

### 步驟 4：開啟瀏覽器

在瀏覽器打開：
```
http://localhost:3000/diary-reward.html
```

**重要**：一定要用 `http://localhost:3000/...` 開啟，不要用 `file:///...`

## 🎮 使用流程

### 1. 每日引導語

- 系統會在設定的時間（預設 22:00）自動顯示引導語
- 引導語會根據你過去 7 天的日記內容個人化生成
- 手動測試：按 F12 打開 Console，輸入 `showGuide()`

### 2. 錄音日記

1. 點擊右上角的語音指示器（三個白色像素點）
2. 允許麥克風權限
3. 開始說話（語音指示器會動畫）
4. 再次點擊停止錄音
5. 系統自動：
   - 上傳日記到 Firebase
   - 獎勵 150 枚金幣
   - 播放金幣掉落動畫

### 3. 金幣堆疊

- 每次錄音獲得 150 枚金幣
- 金幣從天而降，物理引擎自動堆疊
- 當金幣堆到語音指示器高度時，觸發列印

### 4. 個人化鼓勵訊息

- 金幣堆滿後自動觸發
- OpenAI 根據你最近 10 天的日記內容生成專屬鼓勵語
- 顯示在黃色通知框中
- 5 秒後自動關閉，金幣重置

## 🧪 測試 API

### 測試引導語生成

```bash
curl -X POST http://localhost:3000/api/generate-guide \
  -H 'Content-Type: application/json' \
  -d '{"userId":"default-user","daysToAnalyze":7}'
```

預期回應：
```json
{
  "guideText": "今晚想聊聊什麼讓你感到平靜嗎？",
  "basedOnDiaries": 3
}
```

### 測試儲存日記

```bash
curl -X POST http://localhost:3000/api/save-diary \
  -H 'Content-Type: application/json' \
  -d '{
    "userId":"default-user",
    "content":"今天完成了很多事情，感覺很充實。",
    "mode":"text"
  }'
```

預期回應：
```json
{
  "success": true,
  "diaryId": "abc123",
  "coinsEarned": 150
}
```

### 測試列印內容生成

```bash
curl -X POST http://localhost:3000/api/generate-print \
  -H 'Content-Type: application/json' \
  -d '{
    "userId":"default-user",
    "totalCoins":450,
    "daysToAnalyze":10
  }'
```

預期回應：
```json
{
  "printText": "這十天的記錄真實而動人，繼續保持這份對自己的關注。2025/10/29",
  "basedOnDiaries": 5,
  "totalCoins": 450
}
```

## 🎯 Console 測試指令

按 F12 打開瀏覽器 Console，可以使用以下指令：

```javascript
// 查看所有可用指令
help()

// 顯示引導語（會呼叫 API）
showGuide()

// 隱藏引導語
hideGuide()

// 模擬開始錄音
startRecord()

// 模擬停止錄音
stopRecord()

// 增加金幣（n=數量, duration=掉落時間ms）
addCoins(150, 6000)

// 顯示列印通知（會呼叫 API）
showPrint()

// 重置金幣
resetCoins()

// 查看當前狀態
getState()

// 開啟/關閉除錯模式
debug()
```

## 📊 Firebase 資料結構

### 使用者資料

```
users/
  {userId}/
    profile:
      totalCoins: 450
      totalDiaries: 3
      lastDiaryDate: Timestamp
    
    diaries/
      {autoId}:
        date: Timestamp
        dateString: "2025-10-29"
        content: "今天的日記內容..."
        mode: "audio" | "text"
        audioUrl: null | "gs://..."
        duration: 120
        coins: 150
    
    guides/
      {YYYY-MM-DD}:
        guideText: "今晚想..."
        generatedAt: Timestamp
        basedOnDiaries: 7
    
    prints/
      {autoId}:
        printText: "鼓勵訊息..."
        coinsAtPrint: 450
        diariesAnalyzed: 10
        generatedAt: Timestamp
```

## ⚙️ 設定調整

### 修改引導語時間

在 Console 輸入：
```javascript
setGuide(22, 0)  // 設定為 22:00
```

### 啟用/關閉排程

```javascript
enableGuide(true)   // 啟用
enableGuide(false)  // 關閉
```

### 清除今日已顯示記錄

```javascript
resetGuideToday()
```

## 🐛 常見問題

### Q: 看到 "CORS request not http" 錯誤
**A:** 請確保用 `http://localhost:3000/diary-reward.html` 開啟，不要用 `file:///...`

### Q: API 回傳 "Missing OPENAI_API_KEY"
**A:** 
1. 檢查 `.env` 檔案是否存在且包含 `OPENAI_API_KEY=...`
2. 重新啟動 `node server.js`
3. 確認終端機顯示「✅ 已載入環境變數」

### Q: 錄音沒有反應
**A:**
1. 確認瀏覽器允許麥克風權限
2. 檢查 Console 是否有錯誤訊息
3. 嘗試用 Chrome/Firefox（不要用 Safari）

### Q: Firebase 寫入失敗
**A:**
1. 檢查 `.env` 中的 Firebase 設定是否正確
2. 確認 `FIREBASE_PRIVATE_KEY` 包含完整的 BEGIN/END 標記
3. 到 Firebase Console 檢查 Firestore 規則是否允許寫入

### Q: 引導語/鼓勵語都是預設內容
**A:**
1. 先錄幾次日記累積資料
2. 檢查 Console 看 API 回應中的 `basedOnDiaries` 數字
3. 到 Firebase Console 確認 `users/{userId}/diaries` 有資料

## 🔄 日常使用流程

1. **晚上 22:00** → 系統自動顯示引導語
2. **點擊語音指示器** → 開始錄音
3. **說完再次點擊** → 停止並上傳
4. **觀看金幣掉落** → 視覺化獎勵
5. **重複 2-4** → 累積金幣
6. **金幣堆滿** → 自動生成個人化鼓勵訊息

## 📝 開發者備註

- 前端：`diary-reward.html` 包含完整 UI 與邏輯
- 後端 API：
  - `api/save-diary/index.js` - 儲存日記
  - `api/generate-guide/index.js` - 生成引導語
  - `api/generate-print/index.js` - 生成鼓勵訊息
- 本地伺服器：`server.js` - 提供靜態檔案與 API 路由
- 使用者 ID：目前預設為 `default-user`，可改為從登入系統取得

---

**啟動日期**：2025-10-29  
**狀態**：✅ 完整整合 OpenAI + Firebase + 錄音功能

