# 🪙 金幣日記獎勵系統 - 整合完成

## ✅ 已完成的工作

### 1. API 整合 (/api)

#### `api/save-diary/index.js` - 儲存日記
- ✅ 接收前端錄音或文字內容
- ✅ 寫入 Firebase Firestore (`users/{userId}/diaries`)
- ✅ 更新使用者統計 (`users/{userId}/profile`)
- ✅ 回傳獎勵金幣數

**請求格式:**
```json
{
  "userId": "default-user",
  "content": "今天很充實...",
  "mode": "text" | "audio",
  "audioUrl": null,
  "duration": 120
}
```

**回應格式:**
```json
{
  "success": true,
  "diaryId": "abc123",
  "coinsEarned": 150
}
```

#### `api/generate-guide/index.js` - 生成個人化引導語
- ✅ 從 Firestore 讀取使用者最近 N 天日記
- ✅ 呼叫 OpenAI 根據歷史內容生成引導語
- ✅ 儲存引導語到 Firestore (`users/{userId}/guides`)
- ✅ 回傳引導語與分析筆數

**請求格式:**
```json
{
  "userId": "default-user",
  "daysToAnalyze": 7
}
```

**回應格式:**
```json
{
  "guideText": "今晚想聊聊什麼讓你感到平靜嗎？",
  "basedOnDiaries": 3
}
```

#### `api/generate-print/index.js` - 生成個人化鼓勵訊息
- ✅ 從 Firestore 讀取使用者最近 N 天日記
- ✅ 讀取使用者統計數據
- ✅ 呼叫 OpenAI 根據歷史內容與金幣數生成鼓勵語
- ✅ 儲存列印記錄到 Firestore (`users/{userId}/prints`)
- ✅ 回傳鼓勵語、分析筆數與統計

**請求格式:**
```json
{
  "userId": "default-user",
  "totalCoins": 450,
  "daysToAnalyze": 10
}
```

**回應格式:**
```json
{
  "printText": "這十天的記錄真實而動人，繼續保持這份對自己的關注。2025/10/29",
  "basedOnDiaries": 5,
  "totalCoins": 450
}
```

### 2. 前端整合 (diary-reward.html)

#### 錄音功能
- ✅ Web Audio API 整合
- ✅ MediaRecorder 錄製音訊
- ✅ 錄音開始/停止控制
- ✅ 錄音完成後自動呼叫 `/api/save-diary`
- ✅ 上傳成功後觸發金幣動畫

#### 引導語顯示
- ✅ 定時排程系統（預設 22:00）
- ✅ 呼叫 `/api/generate-guide` 取得個人化引導語
- ✅ 顯示基於歷史日記數量的資訊
- ✅ 錯誤處理與 fallback

#### 鼓勵訊息生成
- ✅ 金幣堆滿時自動觸發
- ✅ 呼叫 `/api/generate-print` 取得個人化鼓勵語
- ✅ 帶入總金幣數與使用者 ID
- ✅ 顯示列印通知框

#### 其他功能
- ✅ 金幣物理引擎動畫
- ✅ 語音指示器 UI
- ✅ Console 測試指令
- ✅ LocalStorage 狀態保存

### 3. 本地伺服器 (server.js)

- ✅ 手動載入 `.env` 環境變數
- ✅ 提供靜態檔案服務
- ✅ API 路由處理
- ✅ CORS 設定
- ✅ 支援 ES6 模組載入

### 4. Firebase 資料結構

```
users/
  {userId}/
    ├── profile (Document)
    │   ├── totalCoins: number
    │   ├── totalDiaries: number
    │   └── lastDiaryDate: Timestamp
    │
    ├── diaries/ (Collection)
    │   └── {autoId} (Document)
    │       ├── date: Timestamp
    │       ├── dateString: "YYYY-MM-DD"
    │       ├── content: string
    │       ├── mode: "text" | "audio"
    │       ├── audioUrl: string | null
    │       ├── duration: number
    │       └── coins: number
    │
    ├── guides/ (Collection)
    │   └── {YYYY-MM-DD} (Document)
    │       ├── guideText: string
    │       ├── generatedAt: Timestamp
    │       └── basedOnDiaries: number
    │
    └── prints/ (Collection)
        └── {autoId} (Document)
            ├── printText: string
            ├── coinsAtPrint: number
            ├── diariesAnalyzed: number
            ├── generatedAt: Timestamp
            └── date: string
```

## 🎯 使用流程

```
1. 使用者看到引導語（定時或手動）
   └─> fetch('/api/generate-guide')
       └─> 讀取 Firebase 歷史日記
           └─> OpenAI 生成個人化引導語

2. 使用者點擊錄音按鈕
   └─> MediaRecorder 開始錄音
       └─> 使用者說話
           └─> 點擊停止
               └─> fetch('/api/save-diary')
                   └─> 寫入 Firebase
                       └─> 更新統計
                           └─> 觸發金幣動畫 (+150)

3. 金幣堆疊到指示器高度
   └─> 自動觸發列印
       └─> fetch('/api/generate-print')
           └─> 讀取 Firebase 歷史日記與統計
               └─> OpenAI 生成個人化鼓勵語
                   └─> 顯示列印通知
                       └─> 儲存列印記錄
                           └─> 重置金幣
```

## 📝 環境變數需求

`.env` 檔案必須包含：

```bash
# OpenAI
OPENAI_API_KEY=sk-...

# Firebase Admin SDK
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
...完整私鑰...
-----END PRIVATE KEY-----"

# Firebase Web SDK (選用)
FIREBASE_API_KEY=...
FIREBASE_AUTH_DOMAIN=...
FIREBASE_STORAGE_BUCKET=...
FIREBASE_MESSAGING_SENDER_ID=...
FIREBASE_APP_ID=...
```

## 🚀 快速啟動

```bash
# 1. 確認環境設定
bash test-setup.sh

# 2. 啟動本地伺服器
node server.js

# 3. 在瀏覽器開啟
# http://localhost:3000/diary-reward.html

# 4. 測試 Console 指令
showGuide()     # 顯示引導語
startRecord()   # 開始錄音
stopRecord()    # 停止錄音
addCoins(150)   # 增加金幣
showPrint()     # 顯示列印通知
```

## 🧪 API 測試指令

```bash
# 測試引導語
curl -X POST http://localhost:3000/api/generate-guide \
  -H 'Content-Type: application/json' \
  -d '{"userId":"default-user","daysToAnalyze":7}'

# 測試儲存日記
curl -X POST http://localhost:3000/api/save-diary \
  -H 'Content-Type: application/json' \
  -d '{"userId":"default-user","content":"測試日記內容","mode":"text"}'

# 測試鼓勵訊息
curl -X POST http://localhost:3000/api/generate-print \
  -H 'Content-Type: application/json' \
  -d '{"userId":"default-user","totalCoins":450,"daysToAnalyze":10}'
```

## 🔄 與舊版的差異

### 舊版 (Pi Story)
- 呼叫 `/api/generatePiStory` 生成故事
- 用於甦醒地圖系統
- 城市/國家為輸入
- 不儲存日記內容

### 新版 (金幣日記)
- 呼叫 `/api/generate-guide` 與 `/api/generate-print`
- 用於日記獎勵系統
- 使用者日記內容為輸入
- **儲存所有日記到 Firebase**
- **根據歷史日記生成個人化內容**
- 整合錄音功能
- 視覺化金幣獎勵系統

## 📊 關鍵差異點

| 項目 | 舊版 Pi Story | 新版金幣日記 |
|------|--------------|-------------|
| API 端點 | `/api/generatePiStory` | `/api/save-diary`, `/api/generate-guide`, `/api/generate-print` |
| 輸入資料 | 城市、國家 | 日記內容、錄音 |
| OpenAI 用途 | 生成故事與問候 | 生成引導語與鼓勵語 |
| Firebase 用途 | 儲存位置記錄 | 儲存日記、引導語、列印記錄 |
| 個人化 | 基於位置 | **基於使用者歷史日記** |
| 前端 | pi-script.js | diary-reward.html |
| 獎勵系統 | 無 | 金幣堆疊動畫 |

## 🎨 設計理念

### 個人化循環
```
日記內容累積 → AI 分析歷史 → 生成個人化引導
     ↑                              ↓
     └──── 使用者看到共鳴內容 ←──────┘
```

### 正向回饋
- 每次記錄 → 立即獲得視覺化獎勵（金幣動畫）
- 累積到量 → 獲得精神獎勵（個人化鼓勵語）
- 持續記錄 → 引導語越來越貼近使用者

### 隱私保護
- 所有日記儲存在使用者自己的 Firebase
- OpenAI 只接收最近 N 天摘要（可控制長度）
- API Key 全在後端，前端無法存取

## 📚 相關文件

- `啟動金幣日記系統.md` - 完整啟動與使用指南
- `diary-reward-api-config.md` - 舊版 API 串接說明（參考用）
- `專案整合說明.md` - 專案演進歷史
- `test-setup.sh` - 環境檢查腳本

## 🔮 未來擴展

- [ ] 語音轉文字（Whisper API）
- [ ] 情緒分析儲存
- [ ] 多使用者登入系統
- [ ] 日記查詢與回顧
- [ ] 匯出功能
- [ ] 樹莓派實體按鈕整合
- [ ] 熱感印表機實體列印

---

**整合完成日期**: 2025-10-29  
**狀態**: ✅ 完整可用
**架構**: OpenAI + Firebase + 錄音 + 金幣動畫

