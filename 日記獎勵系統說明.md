# 每日日記獎勵系統 - 使用說明

## 📋 專案概述

這是一個基於 Raspberry Pi 的網頁應用程式，透過每日日記輸入獲得獎勵，包含：
- ✍️ 文字/語音日記輸入
- 🤖 AI 情緒分析與獎勵計算
- 🪙 硬幣掉落動畫與音效
- 📊 獎勵累積與進度追蹤
- 🎟️ 達標後列印專屬紙籤

## 🗂️ 檔案結構

```
yuhsiang/
├── diary-reward.html           # 主 HTML 文件
├── diary-reward-style.css      # 樣式表
├── diary-reward-script.js      # JavaScript 邏輯
└── 日記獎勵系統說明.md         # 本說明文件
```

## 🚀 快速開始

### 1. 開啟應用程式

```bash
# 方法 1: 直接在瀏覽器開啟
# 使用 Chrome, Firefox 或 Safari 開啟 diary-reward.html

# 方法 2: 使用本地伺服器（推薦）
# 安裝 Python 後執行：
python -m http.server 8000

# 然後在瀏覽器訪問：
# http://localhost:8000/diary-reward.html
```

### 2. 使用介面

#### 📝 文字輸入模式
1. 點擊「📝 文字輸入」按鈕
2. 在文字框中輸入日記內容（至少 20 字）
3. 點擊「✨ 提交日記」

#### 🎤 語音輸入模式
1. 點擊「🎤 語音輸入」按鈕
2. 點擊麥克風圖示開始錄音
3. 說出日記內容
4. 再次點擊停止錄音
5. 點擊「✨ 提交日記」

### 3. 獎勵機制

- **基礎獎勵**: 5-30 枚硬幣（根據內容長度和情緒）
- **累積目標**: 100 枚硬幣
- **達標獎勵**: 解鎖列印專屬任務紙籤

## 🔧 API 整合指南

系統中有 **4 個主要 API 替換位置**，以下詳細說明：

---

### ⚠️ API 替換位置 1: ChatGPT 情緒分析

**檔案**: `diary-reward-script.js`  
**函式**: `analyzeDiaryWithAI(content)`  
**行數**: 約 200-250 行

#### 當前狀態（模擬）
```javascript
async function analyzeDiaryWithAI(content) {
    return new Promise((resolve) => {
        setTimeout(() => {
            // 模擬分析邏輯
            let baseReward = Math.floor(content.length / 10);
            // ...
            resolve(totalReward);
        }, 2000);
    });
}
```

#### 替換為真實 API
```javascript
async function analyzeDiaryWithAI(content) {
    try {
        const response = await fetch('https://api.openai.com/v1/chat/completions', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': 'Bearer YOUR_OPENAI_API_KEY' // ⚠️ 替換為你的 API Key
            },
            body: JSON.stringify({
                model: 'gpt-3.5-turbo',
                messages: [
                    {
                        role: 'system',
                        content: '你是一個情緒分析助手。根據使用者的日記內容，分析情緒並給予 5-30 分的獎勵。正面情緒高給高分，負面情緒給低分但仍鼓勵。只回傳一個 5-30 之間的數字。'
                    },
                    {
                        role: 'user',
                        content: content
                    }
                ],
                max_tokens: 10,
                temperature: 0.7
            })
        });
        
        const data = await response.json();
        const reward = parseInt(data.choices[0].message.content.trim());
        
        // 驗證獎勵範圍
        return Math.min(Math.max(reward, 5), 30);
    } catch (error) {
        console.error('AI 分析錯誤:', error);
        // 失敗時返回基礎獎勵
        return 10;
    }
}
```

#### 設定步驟
1. 註冊 OpenAI 帳號：https://platform.openai.com/
2. 創建 API Key：https://platform.openai.com/api-keys
3. 複製 API Key 並替換上方 `YOUR_OPENAI_API_KEY`
4. **安全提醒**: 不要將 API Key 提交到 Git！使用環境變數或後端代理

---

### ⚠️ API 替換位置 2: 硬幣音效

**檔案**: `diary-reward-script.js`  
**函式**: `playCoinSound()`  
**行數**: 約 350-380 行

#### 當前狀態（Web Audio API）
```javascript
function playCoinSound() {
    // 使用瀏覽器生成簡單音效
    const audioContext = new AudioContext();
    // ...
}
```

#### 替換為音效檔案
```javascript
function playCoinSound() {
    const audio = new Audio('sounds/coin-drop.mp3'); // ⚠️ 準備音效檔案
    audio.volume = 0.5;
    audio.play().catch(e => console.log('音效播放失敗:', e));
}
```

#### 設定步驟
1. 準備音效檔案（推薦格式：MP3 或 WAV）
2. 創建 `sounds/` 資料夾
3. 放置 `coin-drop.mp3` 到資料夾中
4. 調整音量參數 `audio.volume`（0.0-1.0）

**免費音效資源**：
- https://freesound.org/
- https://mixkit.co/free-sound-effects/
- https://www.zapsplat.com/

---

### ⚠️ API 替換位置 3: AI 生成紙籤內容

**檔案**: `diary-reward-script.js`  
**函式**: `generateTicketContent()`  
**行數**: 約 400-430 行

#### 當前狀態（隨機任務）
```javascript
function generateTicketContent() {
    const tasks = [
        '今天嘗試一件從未做過的事',
        // ...
    ];
    const randomTask = tasks[Math.floor(Math.random() * tasks.length)];
    return `任務：${randomTask}\n\n完成後記得寫進日記喔！`;
}
```

#### 替換為 AI 生成
```javascript
async function generateTicketContent() {
    try {
        const response = await fetch('https://api.openai.com/v1/chat/completions', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': 'Bearer YOUR_OPENAI_API_KEY'
            },
            body: JSON.stringify({
                model: 'gpt-3.5-turbo',
                messages: [
                    {
                        role: 'system',
                        content: '你是一個激勵助手。生成一個簡短、正面、可執行的每日任務或勵志小語，約 30 字內，適合列印在紙籤上。'
                    },
                    {
                        role: 'user',
                        content: '請給我一個今日任務或勵志金句'
                    }
                ],
                max_tokens: 100,
                temperature: 0.9
            })
        });
        
        const data = await response.json();
        return data.choices[0].message.content.trim();
    } catch (error) {
        console.error('內容生成錯誤:', error);
        return '今天也要加油喔！💪';
    }
}
```

#### 注意事項
- 此函式需改為 `async`
- 記得在 `showPrintPreview()` 中使用 `await`

---

### ⚠️ API 替換位置 4: Raspberry Pi 熱感印紙機

**檔案**: `diary-reward-script.js`  
**函式**: `confirmPrint()`  
**行數**: 約 460-500 行

#### 當前狀態（模擬列印）
```javascript
async function confirmPrint() {
    closeModal('printModal');
    alert('🖨️ 列印中...');
    // 模擬延遲
    setTimeout(() => {
        alert('✅ 列印完成！');
    }, 2000);
}
```

#### 替換為實際印紙機控制

##### 方法 1: 透過本地 API（推薦）

**後端 API (Python Flask 範例)**：
```python
# printer_api.py
from flask import Flask, request, jsonify
from escpos.printer import Usb

app = Flask(__name__)

@app.route('/api/print', methods=['POST'])
def print_ticket():
    try:
        data = request.json
        content = data.get('content', '')
        
        # 連接印紙機 (請根據你的印紙機調整 vendor_id 和 product_id)
        p = Usb(0x04b8, 0x0e15)
        
        # 列印內容
        p.set(align='center', font='a', bold=True, width=2, height=2)
        p.text('✨ 專屬任務紙籤 ✨\n\n')
        p.set(align='left', font='a', bold=False, width=1, height=1)
        p.text(content + '\n\n')
        p.text('Keep Going! 🌟\n')
        p.cut()
        
        return jsonify({'success': True, 'message': '列印成功'})
    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 500

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
```

**前端呼叫**：
```javascript
async function confirmPrint() {
    closeModal('printModal');
    
    try {
        const response = await fetch('http://localhost:5000/api/print', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                content: document.getElementById('ticketContent').textContent,
                timestamp: new Date().toISOString()
            })
        });
        
        const result = await response.json();
        
        if (result.success) {
            alert('✅ 列印成功！請拿取你的紙籤');
            // 重置獎勵
            AppState.totalRewards = 0;
            AppState.saveState();
            AppState.updateUI();
            document.getElementById('printCard').classList.add('hidden');
        } else {
            alert('❌ 列印失敗：' + result.error);
        }
    } catch (error) {
        console.error('列印錯誤:', error);
        alert('❌ 列印失敗，請檢查印紙機連接');
    }
}
```

##### 方法 2: 直接使用 Python 腳本

**建立 `print_ticket.py`**：
```python
import sys
from escpos.printer import Usb

def print_ticket(content):
    try:
        p = Usb(0x04b8, 0x0e15)
        p.text(content)
        p.cut()
        print("SUCCESS")
    except Exception as e:
        print(f"ERROR: {e}")

if __name__ == "__main__":
    content = sys.argv[1] if len(sys.argv) > 1 else "測試列印"
    print_ticket(content)
```

**前端透過伺服器執行**：
```javascript
async function confirmPrint() {
    const content = document.getElementById('ticketContent').textContent;
    
    try {
        const response = await fetch('/execute-print', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ content })
        });
        // ...
    } catch (error) {
        console.error('列印錯誤:', error);
    }
}
```

---

## 🎮 紅外線感應器整合

### 模擬投幣動作

**目前**: 右下角有「💰 測試投幣」按鈕可觸發動畫

**整合步驟**：

1. **硬體連接**: 將紅外線感應器連接到 Raspberry Pi GPIO

2. **Python 監聽腳本** (`ir_sensor.py`):
```python
import RPi.GPIO as GPIO
import requests
import time

# 設定 GPIO 模式
GPIO.setmode(GPIO.BCM)
IR_PIN = 17  # 紅外線感應器連接的 GPIO 腳位

GPIO.setup(IR_PIN, GPIO.IN)

def on_sensor_trigger():
    # 觸發前端動畫
    try:
        requests.post('http://localhost:8000/trigger-coin-animation')
        print("✅ 投幣動畫已觸發")
    except:
        print("❌ 無法連接前端")

try:
    print("🔍 開始監聽紅外線感應器...")
    while True:
        if GPIO.input(IR_PIN) == GPIO.LOW:  # 偵測到物體
            on_sensor_trigger()
            time.sleep(1)  # 防止重複觸發
except KeyboardInterrupt:
    GPIO.cleanup()
```

3. **後端 API** (加入到 Flask):
```python
@app.route('/trigger-coin-animation', methods=['POST'])
def trigger_animation():
    # 可以透過 WebSocket 或 Server-Sent Events 通知前端
    return jsonify({'success': True})
```

4. **前端接收** (使用 WebSocket):
```javascript
const ws = new WebSocket('ws://localhost:8000/ws');
ws.onmessage = (event) => {
    if (event.data === 'coin-trigger') {
        triggerCoinAnimation(10);
    }
};
```

---

## 📦 依賴套件安裝

### Python 依賴
```bash
# 熱感印紙機
pip install python-escpos

# Flask API
pip install flask flask-cors

# GPIO 控制 (Raspberry Pi)
pip install RPi.GPIO
```

### 印紙機驅動
```bash
# 安裝 CUPS (Linux)
sudo apt-get install cups libcups2-dev

# 查看連接的 USB 裝置
lsusb
```

---

## 🔐 安全性建議

### 1. API Key 保護

**❌ 不要這樣做**:
```javascript
const apiKey = 'sk-1234567890abcdef'; // 明碼寫在前端
```

**✅ 推薦做法**:
```javascript
// 透過後端代理
const response = await fetch('/api/analyze', {
    method: 'POST',
    body: JSON.stringify({ content })
});
```

### 2. 後端代理範例
```python
# backend/api.py
import os
from openai import OpenAI

client = OpenAI(api_key=os.getenv('OPENAI_API_KEY'))

@app.route('/api/analyze', methods=['POST'])
def analyze():
    content = request.json['content']
    response = client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=[...],
    )
    return jsonify({'reward': response.choices[0].message.content})
```

### 3. 環境變數設定
```bash
# .env
OPENAI_API_KEY=sk-your-actual-key-here
```

---

## 🐛 測試與除錯

### 開發者工具

在瀏覽器 Console 中可用：

```javascript
// 填充測試資料
testFillData()

// 測試硬幣動畫
testCoinAnimation()

// 測試列印功能
testShowPrint()
```

### 常見問題

#### 1. 語音識別不工作
- **原因**: 瀏覽器不支援或未授權麥克風
- **解決**: 使用 Chrome，並允許麥克風權限

#### 2. 硬幣動畫卡頓
- **原因**: 一次生成太多硬幣
- **解決**: 限制 `coinCount` 最多 20 個

#### 3. 印紙機無回應
- **原因**: USB 連接或驅動問題
- **解決**: 執行 `lsusb` 檢查裝置，確認 vendor_id 和 product_id

---

## 🎨 自訂設定

### 調整獎勵目標
```javascript
// diary-reward-script.js
const AppState = {
    targetReward: 100,  // 改為你想要的目標值
    // ...
};
```

### 修改顏色主題
```css
/* diary-reward-style.css */
:root {
    --primary-color: #4CAF50;      /* 主色調 */
    --secondary-color: #FF9800;    /* 次要色 */
    --accent-color: #FFD700;       /* 強調色 */
    /* ... */
}
```

### 調整動畫速度
```css
/* 硬幣掉落速度 */
@keyframes coin-fall {
    /* 0% -> 100% 目前是 2s，可以調整 */
}
```

---

## 📱 響應式設計

系統已針對以下尺寸優化：
- ✅ 桌面瀏覽器 (1200px+)
- ✅ 平板 (768px - 1200px)
- ✅ 手機 (< 768px)
- ✅ Raspberry Pi DSI 螢幕 (800x480)

---

## 🚀 部署到 Raspberry Pi

### 1. 傳輸檔案
```bash
# 使用 SCP 傳輸
scp -r diary-reward* pi@raspberrypi.local:~/

# 或使用 Git
git clone https://github.com/your-repo.git
```

### 2. 設定自動啟動
```bash
# 編輯 autostart
nano ~/.config/lxsession/LXDE-pi/autostart

# 加入以下內容
@chromium-browser --kiosk --app=file:///home/pi/diary-reward.html
```

### 3. 啟動後端服務
```bash
# 使用 systemd 設定開機啟動
sudo nano /etc/systemd/system/diary-api.service
```

---

## 📄 授權

本專案採用 MIT 授權條款。

---

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

---

## 📧 聯絡方式

如有問題，請聯絡：[你的聯絡方式]

---

**祝你使用愉快！記得每天寫日記 ✍️**

