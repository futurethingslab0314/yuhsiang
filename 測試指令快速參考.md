# 🎮 Console 測試指令 - 快速參考

## 快速開始

1. 開啟 `diary-reward.html`
2. 按 `F12` 開啟開發者工具
3. 切換到 `Console` 標籤
4. 輸入 `help()` 查看完整指令列表

---

## 🔥 常用測試指令

### 基礎操作

```javascript
help()          // 顯示完整指令列表
debug()         // 開啟/關閉除錯模式（左上角顯示狀態）
getState()      // 查看當前完整狀態
```

### 引導語測試

```javascript
showGuide()     // 顯示引導語
hideGuide()     // 隱藏引導語
```

### 錄音流程測試

```javascript
startRecord()   // 模擬開始錄音
stopRecord()    // 模擬停止錄音（會掉落金幣）
```

### 金幣系統測試

```javascript
addCoins(3)     // 增加 3 枚金幣
addCoins(30)    // 增加 30 枚（達到門檻會觸發列印）
resetCoins()    // 重置金幣數為 0
```

### 列印系統測試

```javascript
showPrint()     // 顯示列印通知
hidePrint()     // 隱藏列印通知
setThreshold(10) // 設定列印門檻為 10 枚
```

---

## 📋 完整測試流程

### 測試 1: 基礎介面

```javascript
// 1. 開啟除錯模式
debug()

// 2. 查看初始狀態
getState()

// 3. 測試引導語
showGuide()

// 等待 3 秒

// 4. 隱藏引導語
hideGuide()
```

### 測試 2: 錄音與獎勵流程

```javascript
// 1. 顯示引導語
showGuide()

// 2. 模擬點擊開始錄音
startRecord()
// 此時語音指示器會動畫，除錯資訊顯示「錄音：是」

// 3. 等待幾秒後停止錄音
stopRecord()
// 會自動掉落 3 枚金幣，除錯資訊顯示金幣數增加
```

### 測試 3: 完整流程（含列印）

```javascript
// 1. 設定較低的列印門檻方便測試
setThreshold(6)

// 2. 開啟除錯模式
debug()

// 3. 重置金幣
resetCoins()

// 4. 模擬多次記錄
stopRecord()  // 第 1 次：3 枚
stopRecord()  // 第 2 次：6 枚（觸發列印！）

// 列印通知會自動顯示
```

### 測試 4: 快速達到列印

```javascript
// 快速測試列印功能
addCoins(30)
// 金幣會快速掉落，2秒後顯示列印通知
```

### 測試 5: 手動觸發列印通知

```javascript
// 直接顯示列印通知（不增加金幣）
showPrint()

// 5秒後會自動隱藏，或手動隱藏
hidePrint()
```

---

## 🎯 測試場景

### 場景 A: 首次使用者體驗

```javascript
// 模擬首次使用者看到的完整流程

// 1. 系統啟動（自動顯示引導語）
// 2. 使用者點擊實體開關（在實際裝置上）
//    或在 Console 輸入：
startRecord()

// 3. 使用者說話...（等待 3-5 秒）

// 4. 再次點擊開關停止
stopRecord()

// 5. 觀察金幣掉落動畫
// 6. 查看當前狀態
getState()
```

### 場景 B: 持續使用達到列印

```javascript
// 模擬使用者累積到列印門檻

debug()              // 開啟除錯
setThreshold(9)      // 設定為 9 枚（3次記錄）

// 第 1 次記錄
stopRecord()         // 3 枚

// 第 2 次記錄
stopRecord()         // 6 枚

// 第 3 次記錄
stopRecord()         // 9 枚（觸發列印！）

// 觀察：列印通知會在金幣掉落 2 秒後顯示
```

### 場景 C: 跨日使用

```javascript
// 模擬跨日使用（金幣保存在 localStorage）

// Day 1
addCoins(15)
getState()           // 查看：totalCoins: 15

// 重新載入頁面（模擬第二天）
location.reload()

// Day 2
getState()           // 金幣數應該保持 15
addCoins(15)         // 再加 15
// 達到 30，觸發列印
```

---

## 🐛 除錯模式說明

開啟除錯模式後，左上角會顯示：

```
狀態: idle / recording / processing / reward / print_ready
金幣數: 0 ~ 30
目標: 30（列印門檻）
錄音: 是 / 否
```

### 狀態說明

- `idle` - 待機中（顯示引導語）
- `recording` - 錄音中
- `processing` - 處理中（上傳錄音）
- `reward` - 獎勵動畫中（金幣掉落）
- `print_ready` - 準備列印（顯示列印通知）

---

## 💡 測試技巧

### 1. 連續測試

```javascript
// 使用 setTimeout 自動化測試
debug()
showGuide()
setTimeout(() => startRecord(), 2000)
setTimeout(() => stopRecord(), 4000)
setTimeout(() => getState(), 6000)
```

### 2. 批次增加金幣

```javascript
// 一次增加多次記錄的金幣
for(let i = 0; i < 10; i++) {
    addCoins(3)
}
// 總共 30 枚，觸發列印
```

### 3. 重置測試環境

```javascript
// 完全重置
resetCoins()
hidePrint()
hideGuide()
getState()
```

### 4. 測試不同門檻

```javascript
// 測試 10 枚門檻
setThreshold(10)
addCoins(10)

// 測試 50 枚門檻
setThreshold(50)
addCoins(50)

// 恢復預設
setThreshold(30)
```

---

## 🎨 視覺測試重點

### 引導語顯示
- ✅ 引導語置中顯示
- ✅ 有淡入動畫
- ✅ 8 秒後自動隱藏
- ✅ 錄音時隱藏

### 金幣掉落
- ✅ 金幣從上方隨機網格位置掉落
- ✅ 金幣是正方形（32x32）
- ✅ 金幣遵循網格對齊（俄羅斯方塊風格）
- ✅ 金幣會堆疊
- ✅ 有垂直網格線

### 語音指示器
- ✅ 平時靜止不動
- ✅ 點擊後開始動畫
- ✅ 放開後停止動畫並掉落金幣

### 列印通知
- ✅ 達到門檻 2 秒後顯示
- ✅ 黃色背景，白色邊框
- ✅ 顯示累積金幣數
- ✅ 5 秒後自動隱藏
- ✅ 隱藏後金幣歸零

---

## 🚨 常見問題排查

### 金幣沒有掉落
```javascript
// 檢查狀態
getState()

// 手動掉落
addCoins(3)
```

### 列印通知沒有顯示
```javascript
// 檢查金幣數和門檻
getState()

// 手動觸發
showPrint()
```

### 除錯資訊沒有更新
```javascript
// 重新開啟除錯模式
debug()
debug()
```

### 引導語沒有顯示
```javascript
// 手動顯示
showGuide()

// 檢查是否被隱藏
document.getElementById('guideMessage').classList.contains('show')
```

---

## 📊 測試檢查表

複製此清單進行測試：

```
基礎功能：
□ 頁面正確載入
□ 金幣掉落動畫正常
□ 金幣是正方形
□ 金幣遵循網格對齊
□ 網格線顯示

引導語：
□ 自動顯示引導語
□ 引導語文字隨機
□ 8秒後自動隱藏
□ 可手動隱藏

錄音流程：
□ startRecord() 開始錄音
□ 語音指示器動畫
□ stopRecord() 停止錄音
□ 停止後掉落金幣
□ 金幣數正確累加

金幣系統：
□ addCoins() 增加金幣
□ 金幣數持久化（重新載入保持）
□ resetCoins() 清空金幣
□ 達到門檻觸發列印

列印系統：
□ 達到門檻自動顯示通知
□ 通知顯示正確金幣數
□ 5秒後自動隱藏
□ 隱藏後金幣歸零

除錯工具：
□ debug() 開啟/關閉
□ 狀態資訊正確顯示
□ 即時更新
□ getState() 顯示完整狀態

Console 指令：
□ help() 顯示指令列表
□ 所有指令都能執行
□ 沒有錯誤訊息
```

---

## 🎓 進階測試

### API 串接測試（需要後端）

```javascript
// 測試 Firebase 上傳（需要先設定 Firebase）
// 實際錄音並上傳
startRecord()
// 說話 5 秒
stopRecord()
// 檢查 Console 是否顯示上傳成功

// 測試 ChatGPT 引導語（需要 API）
// 檢查引導語是否來自 ChatGPT
showGuide()

// 測試列印 API（需要樹莓派 + 印表機）
showPrint()
// 檢查實體印表機是否列印
```

### 效能測試

```javascript
// 測試大量金幣
for(let i = 0; i < 100; i++) {
    addCoins(1)
}
// 觀察動畫是否流暢

// 測試長時間運行
setInterval(() => {
    addCoins(1)
}, 1000)
// 每秒增加 1 枚，觀察記憶體使用
```

---

## 📱 樹莓派測試

在樹莓派上測試時：

```bash
# 1. 啟動網頁伺服器
cd /path/to/project
python3 -m http.server 8000

# 2. 在樹莓派瀏覽器開啟
chromium-browser --kiosk http://localhost:8000/diary-reward.html

# 3. 測試實體開關
# 按下實體按鈕應該觸發 startRecord()
# 放開實體按鈕應該觸發 stopRecord()

# 4. 檢查 Console 輸出
# 按 F12 查看除錯資訊
```

---

祝測試順利！有問題隨時查看 `diary-reward-api-config.md` 🚀

