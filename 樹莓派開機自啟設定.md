# 樹莓派開機自啟設定指南 (Node.js 版本)

本指南將協助您設定樹莓派，使其在開機後自動啟動日記系統伺服器，並自動開啟瀏覽器進入全螢幕模式。

## 步驟 1：設定伺服器開機自啟 (使用 PM2)

PM2 是一個強大的 Node.js 進程管理器，可以輕鬆管理應用程式的背景執行與開機自啟。

1.  **全域安裝 PM2**：
    打開終端機，輸入以下指令：
    ```bash
    sudo npm install -g pm2
    ```

2.  **啟動伺服器**：
    進入您的專案目錄 (例如 `yuhsiang`)，然後啟動伺服器：
    ```bash
    cd ~/yuhsiang  # 請確認這是您的專案路徑
    pm2 start server.js --name "diary-reward"
    ```

3.  **儲存當前執行列表**：
    這會讓 PM2 記住目前正在執行的程式：
    ```bash
    pm2 save
    ```

4.  **設定開機自啟**：
    執行以下指令來產生開機自啟的腳本：
    ```bash
    pm2 startup
    ```
    **注意**：執行上述指令後，終端機會輸出一行類似 `sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u pi --hp /home/pi` 的指令。**請複製並執行該行指令**。

現在，您的伺服器已經設定為開機自啟了！

---

## 步驟 2：設定瀏覽器自動開啟 (Kiosk 模式)

為了讓樹莓派開機後自動顯示日記系統畫面，我們需要設定 Chromium 瀏覽器在桌面啟動時自動執行。

1.  **編輯 Autostart 檔案**：
    輸入以下指令編輯設定檔：
    ```bash
    sudo nano /etc/xdg/lxsession/LXDE-pi/autostart
    ```
    *(如果上述檔案不存在，請嘗試編輯 `~/.config/lxsession/LXDE-pi/autostart`)*

2.  **加入啟動指令**：
    在檔案的**最後面**加入以下幾行：

    ```bash
    @xset s off
    @xset -dpms
    @xset s noblank
    @chromium-browser --kiosk --incognito --noerrdialogs --disable-infobars --check-for-update-interval=31536000 http://localhost:3000/diary-reward.html
    ```

    *   `@xset ...`：這些指令用於關閉螢幕保護程式和電源管理，防止螢幕自動變黑。
    *   `--kiosk`：全螢幕模式（無網址列和邊框）。
    *   `--incognito`：無痕模式（避免快取問題）。
    *   `http://localhost:3000/diary-reward.html`：您的應用程式網址。

3.  **儲存並退出**：
    按 `Ctrl+O` 儲存，按 `Enter` 確認，然後按 `Ctrl+X` 退出。

---

## 步驟 3：重新開機測試

現在，您可以重新啟動樹莓派來測試設定是否成功：

```bash
sudo reboot
```

重開機後，樹莓派應該會：
1.  自動在背景啟動 Node.js 伺服器。
2.  自動開啟 Chromium 瀏覽器並進入日記系統的全螢幕畫面。
3.  實體按鈕功能應該也能正常運作。

## 常見問題

*   **如何停止伺服器？**
    ```bash
    pm2 stop diary-reward
    ```
*   **如何查看伺服器日誌？**
    ```bash
    pm2 logs diary-reward
    ```
*   **如何退出 Kiosk 模式？**
    如果您接了鍵盤，可以按 `Alt+F4` 關閉瀏覽器，或按 `Ctrl+Alt+F2` 進入終端機介面。
