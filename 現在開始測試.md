# 🎉 準備好了！現在可以開始測試

## ✅ 我已經完成的工作

### 1. 建立了 3 個新的 API

- **`api/save-diary/index.js`** - 儲存日記到 Firebase
- **更新 `api/generate-guide/index.js`** - 根據歷史日記生成個人化引導語
- **更新 `api/generate-print/index.js`** - 根據歷史日記生成個人化鼓勵訊息

### 2. 更新了前端 (diary-reward.html)

- ✅ 整合真實的錄音功能 (Web Audio API)
- ✅ 錄音完成後自動上傳到 `/api/save-diary`
- ✅ 引導語呼叫改為帶入 `userId` 和 `daysToAnalyze`
- ✅ 列印通知改為帶入使用者統計與日記分析範圍

### 3. 建立了本地伺服器 (server.js)

- ✅ 手動載入 `.env` 環境變數
- ✅ 支援所有 3 個 API 端點
- ✅ 提供靜態檔案服務

### 4. 文件

- ✅ `啟動金幣日記系統.md` - 完整使用指南
- ✅ `金幣日記系統整合說明.md` - 技術整合說明
- ✅ `test-setup.sh` - 環境檢查腳本

---

## 🚀 現在在樹莓派上這樣做

### 步驟 1：確認 .env 檔案

```bash
cd ~/yuhsiang
cat .env | grep OPENAI_API_KEY
```

應該看到：
```
OPENAI_API_KEY=sk-...
```

如果沒有，請編輯 `.env` 加入你的金鑰。

### 步驟 2：啟動伺服器

```bash
node server.js
```

應該看到：
```
✅ 已載入環境變數
🚀 伺服器已啟動！
📍 網址: http://localhost:3000/diary-reward.html
📡 API: http://localhost:3000/api/...

按 Ctrl+C 停止伺服器
```

### 步驟 3：開啟瀏覽器

在樹莓派的瀏覽器打開：
```
http://localhost:3000/diary-reward.html
```

**重要：不要用 file:// 開啟！**

### 步驟 4：測試功能

#### 方法 A：用 Console 測試

按 F12 打開 Console，輸入：

```javascript
// 1. 測試引導語生成（會呼叫 OpenAI）
showGuide()
```

應該看到：
- 黃底黑字滿版顯示引導語
- Console 顯示：`📝 顯示引導語(From API, 基於 X 筆日記): ...`

```javascript
// 2. 測試錄音（會上傳 Firebase）
startRecord()
// 說幾句話...
stopRecord()
```

應該看到：
- 語音指示器動畫
- Console 顯示：`✅ 日記已儲存: ...`
- 金幣開始掉落

```javascript
// 3. 模擬金幣堆滿
addCoins(300, 3000)
// 等金幣堆到指示器高度...
```

應該看到：
- 自動觸發列印通知
- Console 顯示：`📄 列印內容(From API, 基於 X 筆日記): ...`

#### 方法 B：用 curl 測試 API

開新終端，測試每個 API：

```bash
# 測試引導語
curl -X POST http://localhost:3000/api/generate-guide \
  -H 'Content-Type: application/json' \
  -d '{"userId":"default-user","daysToAnalyze":7}'

# 測試儲存日記
curl -X POST http://localhost:3000/api/save-diary \
  -H 'Content-Type: application/json' \
  -d '{"userId":"default-user","content":"今天很開心","mode":"text"}'

# 測試鼓勵訊息
curl -X POST http://localhost:3000/api/generate-print \
  -H 'Content-Type: application/json' \
  -d '{"userId":"default-user","totalCoins":450}'
```

---

## 🐛 如果遇到問題

### 問題 1：看到 "Missing OPENAI_API_KEY"

**解決方法：**
```bash
# 檢查 .env
cat .env

# 確認有這一行
OPENAI_API_KEY=sk-...

# 重新啟動伺服器
# 按 Ctrl+C 停止，然後
node server.js
```

### 問題 2：瀏覽器顯示 "Cannot GET /diary-reward.html"

**解決方法：**
- 確認在 `~/yuhsiang` 目錄執行 `node server.js`
- 檢查 `diary-reward.html` 檔案是否存在
- 用 `http://localhost:3000/diary-reward.html` 而不是 `file:///...`

### 問題 3：錄音沒反應

**解決方法：**
- 確認瀏覽器有麥克風權限
- 按 F12 看 Console 有沒有錯誤
- 試試用 Chromium（不要用其他瀏覽器）

### 問題 4：Firebase 寫入失敗

**解決方法：**
```bash
# 檢查 Firebase 設定
cat .env | grep FIREBASE

# 應該有這些
FIREBASE_PROJECT_ID=...
FIREBASE_CLIENT_EMAIL=...
FIREBASE_PRIVATE_KEY=...
```

---

## 📊 驗證是否成功

### 1. OpenAI 串接成功

- ✅ Console 呼叫 `showGuide()` 能看到新的引導語（不是預設句）
- ✅ curl 測試 `/api/generate-guide` 能回傳 JSON
- ✅ 金幣堆滿後能看到個人化鼓勵語

### 2. Firebase 串接成功

- ✅ 錄音後 Console 顯示「日記已儲存」
- ✅ 到 Firebase Console 看 `users/default-user/diaries` 有資料
- ✅ curl 測試 `/api/save-diary` 回傳 success: true

### 3. 完整流程成功

1. 引導語顯示 ✅
2. 錄音上傳成功 ✅
3. 金幣掉落動畫 ✅
4. 堆滿後觸發列印 ✅
5. 顯示個人化鼓勵語 ✅

---

## 🎯 下一步

### 如果測試成功

1. 多錄幾次日記，累積資料
2. 隔天看引導語是否更個人化
3. 累積金幣看鼓勵語內容
4. 到 Firebase Console 查看所有資料

### 如果想改進

- 調整引導語分析天數：`daysToAnalyze`
- 修改金幣獎勵數量：`coinsEarned`
- 改變引導語時間：Console 輸入 `setGuide(22, 0)`
- 調整 OpenAI 的 prompt（在各 API 檔案裡）

---

**準備好了嗎？執行 `node server.js` 開始測試吧！** 🚀

